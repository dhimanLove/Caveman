rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // No public access. Add granular rules here if storage is ever used.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
